<Page x:Class="ProjectNetra.Mail.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjectNetra.Mail"
      mc:Ignorable="d" 
      d:DesignHeight="300"
      Title="HomePage" Width="871" Height="411">

    <Grid>
        <ListView Name="foldersList" SelectionChanged="foldersList_SelectionChanged" Margin="5,5,663,10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Margin="10" Cursor="Hand" Text="{Binding Title}" Name="folderTitle" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Frame Name="contentFrame" NavigationUIVisibility="Hidden" Navigated="contentFrame_Navigated" Margin="215,5,5,10" />
    </Grid>
</Page>
